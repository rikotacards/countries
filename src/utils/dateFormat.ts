export const formatOptions = {
  day: "2-digit", // Renders the day as a two-digit number (e.g., '14')
  month: "short", // Renders the month as its full name (e.g., 'December')
  year: "numeric", // Renders the year as a four-digit number (e.g., '2024')
};